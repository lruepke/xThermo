list(APPEND TEST_ITEMS test_H2O test_H2ONaCl)
foreach(TEST_ITEM IN LISTS TEST_ITEMS)
    if(EXISTS ${CMAKE_BINARY_DIR}/lib${LIB_NAME}.dylib)
        link_directories(${CMAKE_BINARY_DIR})
        enable_testing()
        add_executable(${TEST_ITEM} ${TEST_ITEM}.cpp)
        target_link_libraries(${TEST_ITEM} ${GSL_LIBRARIES_SHARED} ${LIB_NAME} ${LIBomp} ${STATIC_LIBS_IAPWS_OTHERS})
        set_target_properties(${TEST_ITEM} PROPERTIES RUNTIME_OUTPUT_DIRECTORY ${CMAKE_BINARY_DIR})
    endif()
endforeach()